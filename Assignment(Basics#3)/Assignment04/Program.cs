class Program
{
    static void Main()
    {
        #region Q17(Create a program that asks the user to input three points (x1, y1), (x2, y2), and (x3, y3), and determines whether these points lie on a single straight line.)
        //int[] x= new int[3];
        //int[] y = new int[3];
        //int[] m = new int[2];
        //  for (int i = 0; i < x.Length; i++) 
        //  {
        //      Console.WriteLine($"Enter the X-coordinate of Point {i+1} ");
        //      int.TryParse(Console.ReadLine(), out x[i]);
        //      Console.WriteLine($"Enter the Y-coordinate of Point {i + 1} ");
        //      int.TryParse(Console.ReadLine(), out y[i]);
        //  }
        //  for (int i = 0; i < x.Length - 1; i++)
        //  {
        //      m[i] = (y[i + 1] - y[i]) / (x[i + 1] - x[i]);
        //  }
        //  if (m[0] == m[1])
        //  {
        //      Console.WriteLine("The points lie on a single straight line.");
        //  }
        //  else
        //  {
        //      Console.WriteLine("The points do not lie on a single straight line.");
        //  }
        #endregion
        #region Q18 (Write a program to determine the efficiency of the worker in a company)
        //double x;
        //Console.WriteLine("Enter the number of hours worked by the worker");
        //double.TryParse(Console.ReadLine(), out x);
       
        //if (x <= 3 && x>0)
        //{
        //    Console.WriteLine("The worker is highly efficient");
        //}
        //else if (x > 3 && x <= 4)
        //{
        //    Console.WriteLine("The worker is ordered to improve speed");
        //}
        //else if(x > 4 && x <= 5)
        //{
        //    Console.WriteLine("The worker is given training to improve speed");
        //}
        //else if (x > 5)
        //{
        //    Console.WriteLine("The worker have to leave the company");
        //}
        //else
        //{
        //     Console.WriteLine("Invalid Input"); 
        //}
        #endregion 

    }
}